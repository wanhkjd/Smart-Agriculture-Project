<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.suoduan.mapper.UserMapper">

    <select id="findByUsername" resultType="com.suoduan.entity.User">
        SELECT id, username, password, email FROM user WHERE username = #{username}
    </select>

    <select id="findById" resultType="com.suoduan.entity.User">
        SELECT id, username, password, email FROM user WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, email) VALUES (#{username}, #{password}, #{email})
    </insert>

    <update id="update">
        UPDATE user
        <set>
            <if test="password != null and password != ''">password = #{password},</if>
            <if test="email != null">email = #{email},</if>
        </set>
        WHERE id = #{id}
    </update>

</mapper>
